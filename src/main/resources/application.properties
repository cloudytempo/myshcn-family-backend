# This file mirrors `application.yml` but uses properties format.
# It intentionally references environment variables so credentials are not committed.

spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://${SUPABASE_DB_HOST:db.rpamiahqvroigntyyggc.supabase.co}:${SUPABASE_DB_PORT:5432}/${SUPABASE_DB_NAME:postgres}?sslmode=require}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${SUPABASE_DB_USER:postgres}
spring.datasource.password=${SUPABASE_DB_PASSWORD:Puchong@9290}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

server.port=8080

# logging
logging.level.org.springframework=INFO

